const NAME = "Admin Users";

export const getIconFromRole = (role) =>
    {
        switch(role){
            case "iconRol": return "asd";
                        break;
            default:
        }
    };

export default NAME;